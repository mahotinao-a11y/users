meta {
  name: Get users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/public/v2/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.length: gte 1
}

settings {
  encodeUrl: true
  timeout: 0
}
